exports.handler = async (event) => {
  const data = JSON.parse(event.body);
  const inputText = data.message
  return {
    statusCode: 200,
    body: JSON.stringify({ message: `You sent: ${inputText}` })
  };
};
