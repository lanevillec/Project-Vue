exports.handler = async (event) => {
  console.log("Received input:", JSON.parse(event.body).input);

  return {
    statusCode: 200,
    body: JSON.stringify({ message: "Input received successfully" })
  };
};
